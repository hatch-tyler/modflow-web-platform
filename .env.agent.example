# PEST++ Agent Configuration
# Copy this file to .env on helper machines and modify as needed

# Main server IP address (where the web app is running)
MANAGER_HOST=192.168.1.100

# PEST++ manager port (must match the main server's PEST_MANAGER_PORT)
MANAGER_PORT=4004

# MinIO server for model file sync (usually same as MANAGER_HOST)
MINIO_HOST=192.168.1.100
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# Number of agents to run on this machine
# Recommended: number of CPU cores - 1
NUM_AGENTS=4

# Optional: Project and Run IDs (set automatically when using the web UI)
# PROJECT_ID=
# RUN_ID=
